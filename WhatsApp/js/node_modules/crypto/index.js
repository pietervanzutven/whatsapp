require((exports, module) => {
  module.exports = window.crypto_browserify;
  module.exports.randomInt = function (max) {
    const seed = new Uint32Array(1);
    crypto.getRandomValues(seed);
    return seed[0];
  }
});